# 股票数据爬取系统使用指南

## 功能概述

本系统提供了完整的A股股票数据爬取和管理功能，包括：

1. **股票数据爬取** - 从上交所和深交所获取股票基本信息
2. **数据存储管理** - 支持内存数据库和文件存储
3. **API接口** - 提供RESTful API进行数据查询和管理
4. **实时同步** - 自动同步到交易品种数据库

## 文件说明

### 核心文件

- `stock_crawler.py` - 股票数据爬取脚本
- `simple_backend.py` - 后端API服务（已集成股票管理功能）
- `test_stock_api.py` - API测试脚本

### 数据文件

- `a_stock_list.csv` - 爬取的股票数据（CSV格式）
- `a_stock_list.json` - 爬取的股票数据（JSON格式）

## 快速开始

### 1. 独立运行爬虫

```bash
# 直接运行爬虫脚本
python3 stock_crawler.py
```

输出示例：
```
==================================================
A股股票数据爬取工具
==================================================
开始爬取A股数据...
正在获取上交所股票数据...
使用备用上交所数据...
获取上交所股票 10 只
正在获取深交所股票数据...
使用备用深交所数据...
获取深交所股票 8 只
总计获取股票数据 18 只

前10条数据预览:
--------------------------------------------------------------------------------
代码         名称              上市日期         所属市场      
--------------------------------------------------------------------------------
600000     浦发银行            1999-11-10   上海证券交易所   
600036     招商银行            2002-04-09   上海证券交易所   
600519     贵州茅台            2001-08-27   上海证券交易所   
...

数据已保存到 a_stock_list.csv
数据已保存到 a_stock_list.json

统计信息:
上交所股票: 10 只
深交所股票: 8 只
总计: 18 只
```

### 2. 启动后端服务

```bash
# 启动FastAPI后端服务
python3 simple_backend.py
```

服务将在 `http://localhost:8000` 启动

### 3. 通过API管理股票数据

#### 导入股票数据
```bash
curl -X POST "http://localhost:8000/api/v1/stocks/import" \
  -H "Content-Type: application/json" \
  -d '{"stocks": [...]}'
```

#### 运行爬虫API
```bash
curl -X POST "http://localhost:8000/api/v1/stocks/crawler/run"
```

#### 获取股票列表
```bash
curl "http://localhost:8000/api/v1/stocks/a-stocks?limit=10"
```

#### 获取单只股票详情
```bash
curl "http://localhost:8000/api/v1/stocks/a-stocks/600519"
```

#### 获取统计信息
```bash
curl "http://localhost:8000/api/v1/stocks/stats"
```

### 4. 运行测试脚本

```bash
# 运行完整的API测试
python3 test_stock_api.py
```

## API接口详细说明

### 股票数据管理

| 接口 | 方法 | 描述 |
|------|------|------|
| `/api/v1/stocks/a-stocks` | GET | 获取A股列表 |
| `/api/v1/stocks/a-stocks/{code}` | GET | 获取单只股票详情 |
| `/api/v1/stocks/import` | POST | 导入股票数据 |
| `/api/v1/stocks/crawler/run` | POST | 运行爬虫 |
| `/api/v1/stocks/stats` | GET | 获取统计信息 |

### 品种搜索

| 接口 | 方法 | 描述 |
|------|------|------|
| `/api/v1/symbols/search` | GET | 搜索交易品种 |
| `/api/v1/symbols/categories` | GET | 获取品种分类 |

## 数据结构

### 股票数据格式

```json
{
  "code": "600519",
  "name": "贵州茅台",
  "full_name": "贵州茅台酒股份有限公司",
  "list_date": "2001-08-27",
  "industry": "白酒",
  "area": "贵州",
  "market": "SH",
  "exchange": "上海证券交易所"
}
```

### 字段说明

- `code`: 股票代码（6位数字）
- `name`: 股票简称
- `full_name`: 公司全称
- `list_date`: 上市日期（YYYY-MM-DD格式）
- `industry`: 所属行业
- `area`: 所在地区
- `market`: 市场标识（SH=上海，SZ=深圳）
- `exchange`: 交易所名称

## 数据源说明

### 当前数据源

由于官方接口访问限制，系统当前使用备用数据源：

1. **上交所备用数据** - 包含10只代表性股票
2. **深交所备用数据** - 包含8只代表性股票

### 扩展数据源

系统设计支持多数据源，未来可扩展：

1. **官方API** - 上交所、深交所官方接口
2. **第三方API** - Tushare、东方财富等
3. **网页爬取** - 财经网站股票列表

## 功能特性

### 1. 数据去重和清洗

- 自动去除重复股票
- 验证股票代码格式
- 标准化日期格式
- 数据完整性检查

### 2. 多格式输出

- JSON格式 - 用于API交互
- CSV格式 - 便于Excel处理
- 内存数据库 - 快速查询

### 3. 实时同步

- 自动同步到交易品种数据库
- 支持增量更新
- 保持数据一致性

### 4. 统计分析

- 按市场分布统计
- 按行业分类统计
- 按地区分布统计

## 故障排除

### 常见问题

1. **网络连接超时**
   - 检查网络连接
   - 使用备用数据源

2. **依赖库缺失**
   - 安装requests: `pip install requests`
   - 其他依赖按需安装

3. **API接口403错误**
   - 检查后端服务是否启动
   - 确认端口8000未被占用

4. **数据文件未生成**
   - 检查写入权限
   - 确认目录存在

### 日志输出

系统提供详细的日志输出，帮助诊断问题：

```
正在获取上交所股票数据...
上交所接口暂时无法访问，使用备用数据源...
使用备用上交所数据...
📊 保留事实性新闻: ...
✅ 获取上交所股票 10 只
```

## 生产环境部署

### 环境要求

- Python 3.7+
- FastAPI
- requests
- uvicorn

### 配置建议

1. **数据库** - 使用PostgreSQL或MySQL替代内存存储
2. **缓存** - 添加Redis缓存层
3. **监控** - 集成日志和监控系统
4. **安全** - 添加API认证和限流

### 性能优化

1. **并发处理** - 使用异步请求
2. **数据分页** - 大数据量分页查询
3. **缓存策略** - 合理设置缓存时间
4. **连接池** - 数据库连接池优化

## 更新记录

- **v1.0** - 初始版本，支持基本爬取和API功能
- **v1.1** - 添加备用数据源和错误处理
- **v1.2** - 集成到新闻交易系统

## 技术支持

如有问题或建议，请通过以下方式联系：

1. 查看日志输出定位问题
2. 参考API文档确认调用方式
3. 检查网络和服务状态

---

*最后更新：2025-08-01*